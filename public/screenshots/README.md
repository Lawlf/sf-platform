# PWA install screenshots

Drop two PNGs here, captured from the running app:

- `home-narrow.png` — 1080x1920 (mobile portrait), the dashboard with real-looking macro data.
- `home-wide.png` — 1920x1080 (desktop), same dashboard.

Referenced by `app/manifest.ts` `screenshots`. Until present, Chrome simply skips the rich install card; no error. Use illustrative/generic figures, never real user numbers.
